export interface dataStrength {
    title: string
    label: string
    decscript: string
    img_bg: {
        url: string
        alt: string
    }
    img_icon: {
        url: string
        alt: string
    }
}